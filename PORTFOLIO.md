# PORTFOLIO — chartwire

> 이 문서는 chartwire 를 중심으로 세 프로젝트를 요약한 포트폴리오입니다. README 는 프로젝트 자체를, 이 문서는 **무엇을 왜 만들었고 무엇이 실측됐는지**를 설명합니다.

<https://github.com/sokldjs554>

> 모든 수치는 각 저장소의 산출물(JSON/리포트)에서 나온 **실측값**입니다. 포트폴리오·README의 숫자는 스크립트가 채우고 CI가 문서·산출물 불일치를 검사합니다. 달성하지 못한 목표는 미달로 표기했습니다.

---

## 0. 한눈에 보기

| 프로젝트 | 한 줄 | 규모 | 다루는 역량 |
|---|---|---|---|
| **chartwire** (신규) | 정신과 진료실 실시간 음성차팅 백엔드 — 무손실 WebSocket 스트리밍, RLS 멀티테넌시, 동의 기반 파기 영수증, 근거 검증형 SOAP 초안 | 44,032줄 · 테스트 1,154 | REST+WebSocket 실시간, FastAPI, PostgreSQL 설계·ORM, Redis, 실행계획 최적화, AWS 배포, 운영 |
| **aegis-sql** | 한국어 금융 Text-to-SQL 엔진 — AST 거버넌스, 하이브리드 스키마 링킹, 캐스케이드 라우터, 밑바닥부터 만든 sLLM | 26,351줄 · 테스트 265 | SQL 이해도, 쿼리 생성·검증, FastAPI(SSE), 벡터DB, 평가 설계 |
| **DeFactoRule** | 규제 선례 적용 가능성 판단 AI Agent — 문서 구조화, temporal RAG, 결정론적 게이트, 기권 | 24,000줄 · 테스트 581 | 문서 파이프라인, LLM 출력 검증, 실패 사례 관리, 배포(Render) |

세 프로젝트를 관통하는 원칙 하나: **LLM 출력은 신뢰하지 않는 입력이고, 안전 경로에는 결정론적 코드만 둔다.**

---

# 1. chartwire

**저장소** <https://github.com/sokldjs554/chartwire>
**한 줄** SOAPY-class 제품이 30 → 300 의원으로 갈 때 그 아래에 있어야 하는 신뢰성·규정 준수 계층
**스택** Python 3.11 · FastAPI · WebSocket · SQLAlchemy 2.0 async · PostgreSQL 16(RLS·파티셔닝) · Redis 7(Streams·pub/sub·Lua) · Alembic · AWS CDK · Docker · GitHub Actions

## 1.1 문제 정의

음성차팅 제품은 STT 모델 때문에 실패하지 않습니다. 아래에서 실패합니다.

| # | 문제 | chartwire의 답 |
|---|---|---|
| 1 | 진료실 Wi-Fi가 끊겨 오디오가 유실·중복된다 | seq / 누적 ack / credit 백프레셔 / resume / epoch 펜싱 프로토콜. **ack = PostgreSQL에 durable** |
| 2 | 09:00에 여러 의원이 동시 시작 → STT 큐 적체 → 메모리 폭주 | STT 지연과 원장 적체를 반영한 credit 계산, `pause`, `MAXLEN` 스트림 |
| 3 | 자살 사고 발화는 초 단위로 임상의에게 닿아야 한다 | stt-worker 트랜잭션 안의 결정론적 탐지 → 즉시 발행 → SLA 타이머·에스컬레이션 |
| 4 | LLM 초안이 아무도 말하지 않은 약을 적는다 | 문장마다 원문 인용 + 8규칙 결정론적 검증, 스키마에 **진단 필드 없음**, 저커버리지면 기권 |
| 5 | 한 DB를 여러 의원이 쓴다 | PostgreSQL RLS + 단일 `chartwire_app` 역할(NOBYPASSRLS), 라우트×역할×테넌트 매트릭스 |
| 6 | 동의 철회 vs 진료기록 10년 보존 | DEK 파기 + 물리 삭제(파기 영수증) ↔ 기록 키로 봉인된 서명 노트 존속 |
| 7 | 수백만 세그먼트에서 "지난 6개월 불면 언급" | 월 파티션 + keyset, RLS 아래 트라이그램 무력화 → `SECURITY DEFINER` 해법 |
| 8 | 라이브 세션을 떨어뜨리지 않고 배포한다 | drain, 트랜잭션 아웃박스(리스·재시도·DLQ), 메트릭 27종, 런북 |

## 1.2 아키텍처

```
녹음기(태블릿/콘솔) ──WS /ws/v1/ingest──┐
                                        ▼
                        ┌──────── api (FastAPI) ────────┐
  임상의 뷰어 ──WS /watch─┤ REST 34 · ws ingest/watch     │
                        │ RLS 컨텍스트 · 원장 배처       │
                        └──┬──────────────┬──────────────┘
              SQL(asyncpg) │              │ XADD sess:{id}:chunks
                           ▼              ▼
            PostgreSQL 16 (RLS·파티션·아웃박스·감사)   Redis 7 (Streams·pub/sub·ZSET·Lua)
                           ▲              │ XREADGROUP
             아웃박스 폴링 │              ▼
                    ┌──────┴──────┐  ┌────────────┐
                    │ worker      │  │ stt-worker │─ 어댑터: 시뮬레이터 │ AWS Transcribe
                    │ 노트·파기·  │  │ 전사·위험  │
                    │ SLA·파티션  │  │ 탐지 트랜잭션│
                    └─────────────┘  └────────────┘
             오브젝트 스토어: LocalFs(dev) / S3+KMS(prod) — 세션 DEK 봉투 암호화
```

## 1.3 실측 수치

### 프로토콜·부하 (같은 호스트, 4 vCPU, loopback, STT 시뮬레이터, 클라이언트 혼입)

| 시나리오 | ack p50 / p95 | final e2e p95 | 경보 e2e p95 | 유실 / 중복 |
|---|---|---|---|---|
| A N=50 (250 chunk/s) | **81 / 223 ms** | 208 ms | 56 ms | **0 / 0** |
| A N=100 (500 chunk/s) | **182 / 449 ms** | 893 ms | 65 ms | **0 / 0** |
| A N=200 (목표 1,000 chunk/s) | 4,512 / 9,036 ms | 8,879 ms | 230 ms | 0 / 0 ※ |

- ※ **A N=200은 무릎(knee)이고 목표 미달로 기록했습니다.** 실측 790 chunk/s, 200세션 중 **158세션만 종료** — 나머지 42세션은 `hello`가 시간 안에 돌아오지 않아 청크를 한 개도 보내지 못했습니다. 즉 이 행의 `유실 0`은 **시작된 세션이 보낸 청크**에 대한 값이지 시스템 전체 주장이 아닙니다(시작된 세션은 떨어뜨리지 않고 느려지지만, 새 세션은 받지 못합니다). 스레드별 CPU 샘플링으로 단일 이벤트 루프가 한 코어에 고정(100.7 %)되는 것이 병목임을 확인했고, 해법은 코어 증설이 아니라 uvicorn 다중 프로세스 + stt-worker 다중 인스턴스(미실행)입니다.
- **B(STT 400 ms 지연)**: credit이 39.5초에 0으로 수렴, `pause` 90회, 스트림 길이 최대 301(상한 2,000), api RSS 기울기 2.47 MB/min, 유실 0 → **밀려도 메모리가 터지지 않습니다.**
- **C(느린 뷰어 20 %)**: 녹음기 ack p95가 A N=100 대비 −20.9 % → **느린 뷰어가 녹음기를 느리게 만들지 않습니다.**
- **D(카오스: 10초마다 소켓 10 % 강제 종료 · Redis FLUSHDB · stt-worker SIGSTOP 15초)**: resume 성공률 **100 % (140/140)**, 원장 rebuild 57회, **유실 0 · 중복 0**, 세그먼트 연속성 100 %.
- **H(아웃박스 10만 이벤트 · 워커 2 · 1회 SIGKILL)**: **413 events/s, DLQ 0**, 리스 만료 후 1,345건 회수 → 효과는 정확히 한 번.
- 프로토콜 불변식: hypothesis 상태 기반 테스트 **2,300 예제**, 카오스 유실/중복 0.

### 쿼리 실행계획 — 200만 세그먼트 / 120만 검색행 / 200만 아웃박스 (적재 108초)

| 쿼리 | before (0006, RLS만) | after (0007) | 무엇을 보여주는가 |
|---|---|---|---|
| Q1 환자 6개월 타임라인 keyset | 27.4 ms | **2.5 ms** | 파티션 프루닝 + 무중단 생성 파티션 인덱스 |
| Q1a 세션 재생 (프루닝 없음 → 상·하한) | 2.6 ms | **0.98 ms** | 파티션 키를 술어에 넣어야 24개 → 1개 |
| Q2b `ILIKE '%불면증%'` 앱 역할(RLS) | — | 41.2 ms | **RLS 아래에서 트라이그램 GIN이 무시된다** (`texticlike` 비 leakproof) |
| Q2c 같은 쿼리, 소유자 역할 | — | **6.0 ms** | `SECURITY DEFINER` 경계가 해법인 이유 |
| Q2d `search_segments()` 앱 역할 호출 | — | **5.8 ms** | 앱 역할에서의 end-to-end 수정 |
| Q3 미확인 경보 SLA | 1.6 ms | **0.70 ms** | 부분 인덱스 |
| Q4 아웃박스 클레임 (`FOR UPDATE SKIP LOCKED`) | 207.1 ms | **0.45 ms** | 부분 인덱스 + bloat 관찰 |
| Q6 환자 이름 정확 일치 | — | **0.26 ms** | HMAC 블라인드 인덱스(암호화 컬럼인데 검색 가능) |

RLS 오버헤드는 Q1에서 +19.0 %. `pg_proc.proleakproof` 조회 결과와 `EXPLAIN (ANALYZE, BUFFERS)` 원본 JSON을 전부 커밋했습니다.

### 안전 · 검증

| 항목 | 값 | 읽는 법 |
|---|---|---|
| 파기 검증 (세션 50 + 환자 10) | 잔존 행/오브젝트/키 **0/0/0**, unwrap 실패 **100 %**, 복호화 실패 **100 %**, 영수증 검증 **100 %**, 서명 노트 존속 **50/50** | 파기된 DEK로는 열리지 않아야 정상 |
| RLS/RBAC 교차 접근 | 시도 **252**, 누출 **0** (37 라우트 × 5 역할, 교차 테넌트 30회) | `chartwire_app`(NOBYPASSRLS)으로 접속해 측정 |
| 환각 주입 검출 | 7클래스 × 200건, 정상 문장 21,600건 대비 **오탐 0.0 %** | 숫자·약물·부정·화자·진단·조작 인용 |
| 프롬프트 주입 누출 | **0** (20세션, 주입 발화 33건) | 규칙 8이 33/33 차단 |
| 패러프레이즈 오거부율 | **1.44 %** | 정직한 바꿔쓰기를 검증기가 얼마나 거부하나 |
| 근거 초안 | coverage 1.00(구성상), **fact recall 0.582** | coverage 1.0은 자랑이 아님을 문서에 명시 |
| 위험 발화 held-out (n=300) | P **0.763** / R **0.573** / F1 **0.654** | **목표(R ≥ 0.6) 미달**. 안전망 지표이지 임상 정확도 주장 아님 |
| 경보 지연 (커밋 → 뷰어 수신) | p50 **47.4 ms** / p95 **229.8 ms** | 시나리오 A 안에서 측정 |

## 1.4 설계에서 특히 신경 쓴 부분

**동의 · 파기 · 보존 (ADR-0004).** 동의 범위를 `recording / transcription / ai_drafting / search_index` 넷으로 쪼개 파이프라인 단계마다 fail-closed 게이트를 걸었습니다. 철회 시: 진행 중 세션 강제 종료 → 오브젝트 삭제 → 전사·검색·초안 행 삭제 → **세션 DEK 파기** → Redis 키 삭제 → 영수증 해시. 검증 단계가 실제로 unwrap과 복호화를 **시도해서 실패하는 것**을 확인합니다. 반면 임상의가 서명한 노트는 인용문을 자체 포함한 채 테넌트 기록 키로 봉인돼 `legal_hold='medical_record'`, `retention_until = 서명일 + 10년`으로 남습니다. WAL·백업·VACUUM 이전 dead tuple은 이 영수증의 범위 밖이라는 한계도 문서에 적었습니다. 이름은 "증명서"가 아니라 **영수증**입니다.

**안전 경로에 LLM을 두지 않는다 (ADR-0003).** 위험 발화 탐지, PII 마스킹, 동의 게이트, 근거 검증은 전부 결정론적 코드입니다. LLM은 초안 문장을 제안할 뿐이고, 출력 스키마에 `assessment`·`diagnosis`·`verdict` 같은 필드가 **존재하지 않습니다**(추가 키는 스키마 단계에서 거부).

**아웃박스는 at-least-once이고, 그렇게 부릅니다.** 도메인 변경과 이벤트를 같은 트랜잭션에 쓰고, 핸들러 효과와 `processed_events` 삽입도 같은 트랜잭션에 넣어 재전달이 PK 충돌로 흡수되게 했습니다. 런북에 "exactly-once"라고 쓰지 않았습니다 — **효과가 멱등한 at-least-once**입니다.

## 1.5 실패에서 배운 것 (전부 저장소에 기록)

1. **redis-py의 커넥션 풀 100개 상한**이 100세션 초과에서 파이프라인 전체를 무너뜨렸고, 실패한 세션이 `stt:active`에 남아 **다음 측정을 오염**시켰습니다. 재측정 결과 N=200 세그먼트 0 → 8,099건, 시나리오 C 전사 0 → 5,164건. 오염된 측정으로 만든 "느린 뷰어 격리 2.4 %"는 폐기했습니다.
2. **`logging.basicConfig`가 `extra=`를 전부 버려서** 600건의 실패를 로그에서 분류조차 할 수 없었습니다. 관측 가능성은 기능이 아니라 전제조건이라는 걸 이때 배웠습니다.
3. **적대적 리뷰 41건 중 확인된 보안 결함 5건**: 멱등키 재생이 권한 검사를 우회, 경보 ACK의 IDOR(다른 임상의 환자의 자살 위험 경보를 확인·확정 가능), 파기 후 블라인드 인덱스로 실명 확인 가능, 단일 역할 배포에서 평문 전사 테이블 RLS 우회, SQL 오류에 전사 원문 노출. **기존 테스트 두 개가 결함을 정상으로 고정**하고 있었고, 코드를 무르게 하는 대신 테스트를 고쳤습니다.
4. 재현성 결함도 같은 무게로 고쳤습니다: 클린 클론에서 첫 명령이 실패하던 README 5분 경로, docker compose 볼륨 소유권, 워커 헬스체크 포트, perf 스터디가 DB에 확장을 남겨 다른 테스트를 깨뜨리던 문제.

## 1.6 데모

`make demo` → <http://localhost:8000/console> (전부 합성 데이터, API 키 불필요)

README에 **데모 GIF와 스크린샷 6장**이 있습니다: 소개 화면(5분 투어·역할별 계정) → 녹음 시작(seq/ack/credit 게이지) → 실시간 전사 + 위험 경보 배너(SLA 카운트다운·ACK) → SOAP 초안(문장 클릭 시 근거 하이라이트, 미검증 문장 표시, 진단란은 "AI가 작성하지 않는 영역") → 동의 철회 → 파기 영수증(지운 것의 합계·세션별 단계·검증 항목·재계산 해시가 문서 형식으로) → **복호화 시도 실패** 판정 → 운영 지표.

---

# 2. aegis-sql — 한국어 금융 Text-to-SQL 엔진

**저장소** <https://github.com/sokldjs554/aegis-sql> · 26,351줄 · 테스트 265개(실제 DB 대상, 목킹 없음)

자연어 질의를 SQL로 바꾸되, **생성된 SQL을 실행 전에 AST로 검증**하는 엔진입니다. 백엔드 관점에서 의미 있는 부분:

- **SQL 거버넌스**: sqlglot AST를 파싱해 금지 구문·PII 컬럼 접근·행 수 상한을 검사하고, 통과하지 못한 문장은 DB에 도달하지 않습니다. 5.3M 파라미터 sLLM이 무너져 80건이 차단됐을 때도 **유효하지 않은 문장이 DB에 도달한 사례 0건**.
- **하이브리드 검색**: dense + BM25 + 사내 용어사전 + FK 그래프 확장으로 스키마를 링킹. 어블레이션에서 **41개짜리 용어사전이 실행 정확도 10.0 %p**를 만들었습니다(모델 크기가 아니라 도메인 지식이 정확도를 가릅니다).
- **캐스케이드 라우터**: 질문 난이도를 예측해 template / LLM 티어로 보냅니다. holdout AUC **0.937**, 보정 후 ECE 0.099 → **0.044**. Keras로 학습하고 numpy 가중치로 서빙(서빙 경로에 TF 없음), 추론 오차 5.96e-08.
- **데이터 플라이휠**: 스키마만으로 12,540쌍을 생성(실행 검증·중복 제거·난이도 균형), **train↔test 스켈레톤 누수 0건**.
- **밑바닥부터 만든 sLLM**: 바이트 BPE·디코더 트랜스포머(RMSNorm/RoPE/SwiGLU/KV캐시)·LoRA·SFT·DPO를 직접 구현. **그리고 실행 정확도 0.0 %라는 결과를 그대로 공개**했습니다 — 증명하려던 것은 정확도가 아니라 "학습이 수렴하고, 실패해도 시스템이 안전하게 degrade한다"였습니다.

---

# 3. DeFactoRule — 규제 선례 적용 가능성 판단 Agent

**저장소** <https://github.com/sokldjs554/DeFactoRule> · 24,000줄 · 테스트 581개 · 라이브 데모 <https://defactorule-kl5c.onrender.com>

금융규제 사례집 PDF 1,095건을 구조화하고, 새 질의에 대해 **"가장 비슷한 선례"가 아니라 "이 선례를 지금 적용해도 되는가"**를 판단합니다.

- **문서 파이프라인**: PDF → 사례 → 요청·회답 → 라벨. 스캔 입력은 OCR 분기 후 스키마/인용/OCR 신뢰도 검증을 거쳐야 downstream으로 갑니다(60문서 × 3품질 벤치마크).
- **미래 선례 차단**: 검색 후보를 시간순으로 먼저 거르는 temporal eligibility. 클린 프로토콜에서 **시간 위반 0건**.
- **결정론적 게이트 + 기권**: LLM은 두 요청문의 조건만 구조화하고, 결정적 차이는 코드가 원문 대조로 검증합니다. LLM 출력 스키마에 `verdict`·`applies` 필드가 없습니다.
- **결과**: test 168건 중 **76건 답변 / 92건 기권, 답변 정확도 82.89 %, coverage 45.24 %**. 억지로 회수하지 않은 fail-closed 결과이고, "과잉 기권이 현재 가장 큰 약점"이라고 README에 적었습니다.
- **실측된 함정**: 가장 닮은 선례를 그대로 따르는 baseline은 결론이 갈리는 TRAP 구간에서 정확도 **0.000**이었습니다.

---

# 4. 백엔드 직무 요건 ↔ 근거 매핑

| 요건 | 근거 |
|---|---|
| TypeScript / Python 등 언어 | Python 3.11(전 프로젝트, 타입힌트 + mypy strict 모듈) · 콘솔은 빌드 없는 바닐라 JS |
| **AI 도구 및 AI 에이전트를 활용한 개발·문제 해결** | chartwire를 병렬 에이전트 8개로 구축(계약 우선 고정 → 테스트·mypy·실측으로 검증). 방법과 실제 버그 저널: `docs/AGENTS.md` |
| HTTP·웹 서비스 동작 이해 | WebSocket 프로토콜 자체 설계(프레이밍·close·오류 코드 14종(4xxx 13 + 1012)·heartbeat·drain), RFC 9457 problem+json, CORS·CSP·보안 헤더 |
| REST API 설계·구현 | 34개 엔드포인트, keyset 페이지네이션, `Idempotency-Key`, 레이트 리밋, 역할 매트릭스 테스트 |
| 관계형 DB / SQL 이해 | 20 테이블·7 마이그레이션·RLS·파티셔닝, 실행계획 8종 before/after, aegis-sql의 AST 검증 |
| Git 협업 | 의미 단위 커밋, PR 없는 단독 프로젝트여도 커밋 메시지에 원인·근거·검증 방법 기록 |
| 기획→개발→배포 A to Z | 설계서 → 8 작업 패키지 → 통합 → 측정 → 문서 → 적대적 리뷰 → 수정 (chartwire), Render 배포 (DeFactoRule) |
| FastAPI | 3개 프로젝트 전부 |
| PostgreSQL 설계·ORM | SQLAlchemy 2.0 async, Alembic 왕복 검증, RLS·파티션·트리거 |
| Redis 등 인메모리 DB | Streams·pub/sub·ZSET·Lua·토큰 버킷·멱등키 |
| SQL 실행계획 분석·쿼리 성능 개선 | §1.3 표 (207 ms → 0.45 ms 등), `docs/perf/plans/*.json` 원본 커밋 |
| 헬스케어·AI 서비스 관심 | 의료법 보존 vs 파기 설계, 위험 발화 SLA, PHI 봉투 암호화, 감사 로그 |
| 더 나은 코드·서비스 구조 고민 | ADR 5건, 결정론적 검증기, fail-closed 원칙, "숫자는 손으로 적지 않는다" 파이프라인 |
| 새로운 AI 기술 학습 + AI 서비스·백엔드 아키텍처 설계 참여 희망 | 세 프로젝트 모두 LLM을 제품에 넣되 **신뢰하지 않는 입력으로 다루는 구조**를 설계 |

---

# 5. 정직하게 적는 미실행·한계

- **chartwire**: AWS는 CDK 합성·cdk-nag 검증만 했고 실제 계정 배포는 하지 않았습니다. STT는 시뮬레이터이며 **STT 품질과 노트 품질은 평가하지 않았습니다**. A N=200은 스펙 기대치 미달(무릎)이고, 위험 발화 held-out 재현율 0.573은 목표 0.6 미달입니다. 시나리오 E(다중 프로세스 drain)와 Anthropic 프로바이더는 미실행입니다. 부하 수치는 클라이언트가 같은 호스트에서 돈 **client-confounded** 값입니다.
- **합성 데이터의 상한**: 모든 환자·상담은 합성이며, in-grammar 지표(P/R 1.0)는 생성기와 탐지기가 같은 세계를 공유해 구성상 높습니다. 그래서 **손으로 쓴 held-out 300문장**을 별도로 만들고(탐지기 작성 전에, 탐지기 코드를 열지 않고) 그 수치를 헤드라인으로 씁니다.
- **aegis-sql**: 자체 sLLM의 실행 정확도는 0.0 %입니다(가드가 100 % 차단). 캐스케이드는 LLM 단독 대비 −5.6 %p이며 원인이 라우터 임계값임을 어블레이션으로 밝혔습니다.
- **DeFactoRule**: coverage 45 %는 절반 이상을 사람에게 넘긴다는 뜻이고, 과잉 기권이 최대 약점입니다.

---

GitHub <https://github.com/sokldjs554>
