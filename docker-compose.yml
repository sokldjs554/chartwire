# 개발/데모용 compose (spec §12.2). 모든 데이터는 합성(SYNTHETIC)입니다 — 실제 환자 정보 없음.
# 런타임 프로세스(api, worker, stt-worker)는 chartwire_app 으로만 접속한다; 마이그레이션만 chartwire_owner.
x-app-env: &app-env
  CHARTWIRE_DATABASE_URL: postgresql+asyncpg://chartwire_app:chartwire_app@postgres:5432/chartwire
  CHARTWIRE_DATABASE_OWNER_URL: postgresql+psycopg://chartwire_owner:chartwire_owner@postgres:5432/chartwire
  CHARTWIRE_REDIS_URL: redis://redis:6379/0
  CHARTWIRE_OBJECTSTORE: localfs:/var/lib/chartwire/objects
  CHARTWIRE_KEK_MASTER: ${CHARTWIRE_KEK_MASTER:-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=}
  CHARTWIRE_JWT_SECRET: ${CHARTWIRE_JWT_SECRET:-dev-secret-change-me}
  CHARTWIRE_NOTE_PROVIDER: ${CHARTWIRE_NOTE_PROVIDER:-extractive}
  CHARTWIRE_SCRIPTS_DIR: /var/lib/chartwire/scripts   # seed --demo 가 쓰고 stt-worker 시뮬레이터가 읽는다

x-app: &app
  build: .
  image: chartwire:local
  restart: unless-stopped
  volumes:
    - objects:/var/lib/chartwire/objects
    - scripts:/var/lib/chartwire/scripts
  depends_on:
    migrate:
      condition: service_completed_successfully
    redis:
      condition: service_healthy

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./docker/initdb:/docker-entrypoint-initdb.d:ro   # 01_roles.sql: 두 역할 + chartwire DB + 확장
      - pgdata:/var/lib/postgresql/data
    command: ["postgres", "-c", "shared_preload_libraries=pg_stat_statements"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d chartwire"]
      interval: 5s
      timeout: 3s
      retries: 20
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    command: ["redis-server", "--save", "", "--appendonly", "no"]   # 재구축 가능한 캐시 (ADR-0002)
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    ports:
      - "6379:6379"

  migrate:
    build: .
    image: chartwire:local
    environment: *app-env
    command: ["sh", "-c", "chartwire db upgrade && chartwire seed --demo --if-empty"]
    volumes:
      - scripts:/var/lib/chartwire/scripts
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    <<: *app
    environment:
      <<: *app-env
      CHARTWIRE_ROLE: api
      CHARTWIRE_NODE_ID: api-1
    ports:
      - "8000:8000"          # REST /v1, WS /ws/v1, 콘솔 /console

  # 두 워커는 8000 을 열지 않는다 (ops 서버만 각각 9001 / 9002). 이미지의 HEALTHCHECK 는 8000 을
  # 찌르므로 그대로 두면 두 컨테이너가 영구히 unhealthy 로 표시된다 — 서비스별로 실제 포트를 덮어쓴다.
  worker:
    <<: *app
    environment:
      <<: *app-env
      CHARTWIRE_ROLE: worker
      CHARTWIRE_NODE_ID: worker-1
      CHARTWIRE_WORKER_PORT: "9001"   # /healthz /readyz /metrics
    healthcheck:
      test: ["CMD", "python", "-c", "import sys, urllib.request; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:9001/healthz', timeout=2).status == 200 else 1)"]
      interval: 15s
      timeout: 3s
      start_period: 20s
      retries: 3

  stt-worker:
    <<: *app
    environment:
      <<: *app-env
      CHARTWIRE_ROLE: stt-worker
      CHARTWIRE_NODE_ID: stt-1
      CHARTWIRE_STT_WORKER_PORT: "9002"
    healthcheck:
      test: ["CMD", "python", "-c", "import sys, urllib.request; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:9002/healthz', timeout=2).status == 200 else 1)"]
      interval: 15s
      timeout: 3s
      start_period: 20s
      retries: 3

  # ---- profile `drain` (시나리오 E, 선택): api 2 레플리카 + nginx 업스트림. `docker compose --profile drain up`
  api-drain:
    <<: *app
    profiles: ["drain"]
    environment:
      <<: *app-env
      CHARTWIRE_ROLE: api
    deploy:
      replicas: 2

  nginx:
    image: nginx:1.27-alpine
    profiles: ["drain"]
    volumes:
      - ./docker/nginx/drain.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8080:80"
    depends_on:
      - api-drain

volumes:
  pgdata:
  objects:
  scripts:
